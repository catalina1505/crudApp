(function () {

    'use strict';

    angular
        .module('appModule')
        .controller('AppController',
            Controller);

    Controller.$inject = [
    ];

    function Controller(
        ) 
    {
    }
})();